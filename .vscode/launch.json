{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "vox2organ/main.py",
            "args": ["--train", "--group", "Vox2Cortex Abdomen Patient", "--dataset", "KiTS", "-n", "debug", "--overfit" ],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
        }
    ]
}